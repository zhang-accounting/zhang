import { BalancePadDto } from "../gql/jouralList"

interface Props {
    data: BalancePadDto
}

export default function BalancePadLine({ }: Props) {
    return (
        <div>
            BalancepadLine
        </div>
    )
}