import { BalancePadDto } from "../gql/jouralList"

interface Props {
    data: BalancePadDto
}

export default function BalancePadLine(props: Props) {
    return (
        <div>
            BalancepadLine
        </div>
    )
}